# Training configuration for the baseline.

experiment: churn-baseline
run_name: logreg-baseline
seed: 42

split:
  val_size: 0.15
  test_size: 0.15
  seed: 42

model:
  name: logistic_regression
  max_iter: 1000
  class_weight: null
